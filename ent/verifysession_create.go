// Code generated by ent, DO NOT EDIT.

package ent

import (
	"PluginServer/ent/verifysession"
	"context"
	"errors"
	"fmt"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
)

// VerifySessionCreate is the builder for creating a VerifySession entity.
type VerifySessionCreate struct {
	config
	mutation *VerifySessionMutation
	hooks    []Hook
}

// SetSid sets the "sid" field.
func (vsc *VerifySessionCreate) SetSid(u uuid.UUID) *VerifySessionCreate {
	vsc.mutation.SetSid(u)
	return vsc
}

// SetNillableSid sets the "sid" field if the given value is not nil.
func (vsc *VerifySessionCreate) SetNillableSid(u *uuid.UUID) *VerifySessionCreate {
	if u != nil {
		vsc.SetSid(*u)
	}
	return vsc
}

// SetUserID sets the "user_id" field.
func (vsc *VerifySessionCreate) SetUserID(u uuid.UUID) *VerifySessionCreate {
	vsc.mutation.SetUserID(u)
	return vsc
}

// Mutation returns the VerifySessionMutation object of the builder.
func (vsc *VerifySessionCreate) Mutation() *VerifySessionMutation {
	return vsc.mutation
}

// Save creates the VerifySession in the database.
func (vsc *VerifySessionCreate) Save(ctx context.Context) (*VerifySession, error) {
	vsc.defaults()
	return withHooks(ctx, vsc.sqlSave, vsc.mutation, vsc.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (vsc *VerifySessionCreate) SaveX(ctx context.Context) *VerifySession {
	v, err := vsc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (vsc *VerifySessionCreate) Exec(ctx context.Context) error {
	_, err := vsc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (vsc *VerifySessionCreate) ExecX(ctx context.Context) {
	if err := vsc.Exec(ctx); err != nil {
		panic(err)
	}
}

// defaults sets the default values of the builder before save.
func (vsc *VerifySessionCreate) defaults() {
	if _, ok := vsc.mutation.Sid(); !ok {
		v := verifysession.DefaultSid()
		vsc.mutation.SetSid(v)
	}
}

// check runs all checks and user-defined validators on the builder.
func (vsc *VerifySessionCreate) check() error {
	if _, ok := vsc.mutation.Sid(); !ok {
		return &ValidationError{Name: "sid", err: errors.New(`ent: missing required field "VerifySession.sid"`)}
	}
	if _, ok := vsc.mutation.UserID(); !ok {
		return &ValidationError{Name: "user_id", err: errors.New(`ent: missing required field "VerifySession.user_id"`)}
	}
	return nil
}

func (vsc *VerifySessionCreate) sqlSave(ctx context.Context) (*VerifySession, error) {
	if err := vsc.check(); err != nil {
		return nil, err
	}
	_node, _spec := vsc.createSpec()
	if err := sqlgraph.CreateNode(ctx, vsc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	vsc.mutation.id = &_node.ID
	vsc.mutation.done = true
	return _node, nil
}

func (vsc *VerifySessionCreate) createSpec() (*VerifySession, *sqlgraph.CreateSpec) {
	var (
		_node = &VerifySession{config: vsc.config}
		_spec = sqlgraph.NewCreateSpec(verifysession.Table, sqlgraph.NewFieldSpec(verifysession.FieldID, field.TypeInt))
	)
	if value, ok := vsc.mutation.Sid(); ok {
		_spec.SetField(verifysession.FieldSid, field.TypeUUID, value)
		_node.Sid = value
	}
	if value, ok := vsc.mutation.UserID(); ok {
		_spec.SetField(verifysession.FieldUserID, field.TypeUUID, value)
		_node.UserID = value
	}
	return _node, _spec
}

// VerifySessionCreateBulk is the builder for creating many VerifySession entities in bulk.
type VerifySessionCreateBulk struct {
	config
	builders []*VerifySessionCreate
}

// Save creates the VerifySession entities in the database.
func (vscb *VerifySessionCreateBulk) Save(ctx context.Context) ([]*VerifySession, error) {
	specs := make([]*sqlgraph.CreateSpec, len(vscb.builders))
	nodes := make([]*VerifySession, len(vscb.builders))
	mutators := make([]Mutator, len(vscb.builders))
	for i := range vscb.builders {
		func(i int, root context.Context) {
			builder := vscb.builders[i]
			builder.defaults()
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*VerifySessionMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, vscb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, vscb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, vscb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (vscb *VerifySessionCreateBulk) SaveX(ctx context.Context) []*VerifySession {
	v, err := vscb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (vscb *VerifySessionCreateBulk) Exec(ctx context.Context) error {
	_, err := vscb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (vscb *VerifySessionCreateBulk) ExecX(ctx context.Context) {
	if err := vscb.Exec(ctx); err != nil {
		panic(err)
	}
}
