// Code generated by ent, DO NOT EDIT.

package ent

import (
	"PluginServer/ent/verifysession"
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
)

// VerifySession is the model entity for the VerifySession schema.
type VerifySession struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Sid holds the value of the "sid" field.
	Sid uuid.UUID `json:"sid,omitempty"`
	// UserID holds the value of the "user_id" field.
	UserID       uuid.UUID `json:"user_id,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*VerifySession) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case verifysession.FieldID:
			values[i] = new(sql.NullInt64)
		case verifysession.FieldSid, verifysession.FieldUserID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the VerifySession fields.
func (vs *VerifySession) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case verifysession.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			vs.ID = int(value.Int64)
		case verifysession.FieldSid:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field sid", values[i])
			} else if value != nil {
				vs.Sid = *value
			}
		case verifysession.FieldUserID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field user_id", values[i])
			} else if value != nil {
				vs.UserID = *value
			}
		default:
			vs.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the VerifySession.
// This includes values selected through modifiers, order, etc.
func (vs *VerifySession) Value(name string) (ent.Value, error) {
	return vs.selectValues.Get(name)
}

// Update returns a builder for updating this VerifySession.
// Note that you need to call VerifySession.Unwrap() before calling this method if this VerifySession
// was returned from a transaction, and the transaction was committed or rolled back.
func (vs *VerifySession) Update() *VerifySessionUpdateOne {
	return NewVerifySessionClient(vs.config).UpdateOne(vs)
}

// Unwrap unwraps the VerifySession entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (vs *VerifySession) Unwrap() *VerifySession {
	_tx, ok := vs.config.driver.(*txDriver)
	if !ok {
		panic("ent: VerifySession is not a transactional entity")
	}
	vs.config.driver = _tx.drv
	return vs
}

// String implements the fmt.Stringer.
func (vs *VerifySession) String() string {
	var builder strings.Builder
	builder.WriteString("VerifySession(")
	builder.WriteString(fmt.Sprintf("id=%v, ", vs.ID))
	builder.WriteString("sid=")
	builder.WriteString(fmt.Sprintf("%v", vs.Sid))
	builder.WriteString(", ")
	builder.WriteString("user_id=")
	builder.WriteString(fmt.Sprintf("%v", vs.UserID))
	builder.WriteByte(')')
	return builder.String()
}

// VerifySessions is a parsable slice of VerifySession.
type VerifySessions []*VerifySession
